// Kind can only be CAT (c) or DOG (d) - at least for now
const kindOptions: Array<String> = ["c", "d"];

// Gender can only be MALE (m) or FEMALE (f)
const genderOptions: Array<String> = ["m", "f"];

export { kindOptions, genderOptions };
